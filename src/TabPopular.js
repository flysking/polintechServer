import React from 'react';
import {StyleSheet,View,Text} from 'react-native';
function TabPopular({route}){
    <View style={styles.block}>
        <Text>좋아요 순으로 인기글 표시</Text>
    </View>
}

const styles=StyleSheet.create({
    block:{},
});

export default TabPopular;